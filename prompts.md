# Antigravity AI Prompt Log

The following prompts were used sequentially to instruct the AI agent to scaffold, build, and refine the Incident Management System.

**Phase 1: Database Setup & Refactoring**
1. "Write a docker-compose.yml that includes PostgreSQL, MongoDB, and Redis. Include initialization scripts to create the necessary databases and tables for an Incident Management System."
2. "Refactor the database initialization scripts to match a high-throughput incident management system, rather than a standard ticketing app. In Postgres, replace the current tables with a work_items table (must include a status enum strictly for: OPEN, INVESTIGATING, RESOLVED, CLOSED) and an rca_records table linked to the work item (must include root_cause_category, fix_applied, prevention_steps, and mttr_minutes). In MongoDB, drop the audit logs and attachments collections. Instead, create a raw_signals collection specifically designed to ingest debounced, high-volume JSON error payloads. Ensure there are indexes for efficient timeseries aggregation."

**Phase 2: High-Throughput Ingestion Pipeline**
3. "Now, initialize a Go backend using Gin or Fiber. Create an async ingestion API endpoint designed to receive high-volume JSON error signals. Implement a bounded worker pool and buffered Go channels to process these signals without blocking the main goroutine. Implement a rate-limiter on this endpoint to prevent cascading failures. Add a debouncing mechanism (using an in-memory map with a sync.RWMutex or Redis): If multiple signals for the same component_id arrive within 10 seconds, only generate ONE 'Work Item' in Postgres, but asynchronously dump ALL of the raw JSON payloads into the MongoDB raw_signals collection."
4. "The overall architecture is good, but you missed a few critical requirements from the assignment specification. Please apply the following corrections and then proceed to generate the code: Debouncing: Do NOT use Redis for debouncing. Use an in-memory map protected by Go's native sync.RWMutex. The grading rubric heavily weights the use of native concurrency primitives, so I want to demonstrate that here. Capacity & Limits: A rate limit of 1,000 is too low. The spec requires handling bursts of 10,000 signals/sec. Set the Token Bucket rate limiter to 12,000 req/sec to allow for bursts. Increase the worker pool's channel buffer size to 20,000 to absorb the shock. Observability (Missing Requirement): You must add a /health endpoint. You must also spawn a background goroutine on startup that calculates the current ingestion throughput (Signals/sec) and prints it to the console exactly every 5 seconds. Handler Logic: Ensure the IngestSignal Gin handler strictly returns a 202 Accepted immediately after pushing the payload to the channel, before any DB writes occur. Please generate the Go code with these updates."

**Phase 3: Workflow APIs & Strict State Machine**
5. "The ingestion pipeline looks fantastic. Now we need to implement the Workflow Engine and the APIs that the frontend dashboard will consume, as well as fulfill the Resilience and Testing rubric requirements. Please generate the following: Dashboard APIs: Create Gin endpoints to support the UI: GET /api/v1/incidents (Fetch active incidents, sorted by severity) GET /api/v1/incidents/:id (Fetch details and raw signals from Mongo) POST /api/v1/incidents/:id/rca (Submit an RCA. Must automatically calculate MTTR in minutes based on the first_signal_time and the current time). PATCH /api/v1/incidents/:id/status (Update incident state: OPEN -> INVESTIGATING -> RESOLVED -> CLOSED). State Pattern & Validation: Implement the state transition logic strictly. The system MUST reject any attempt to move a Work Item to CLOSED if its associated RCA record does not exist or is incomplete. DB Retry Logic: Wrap your Postgres and MongoDB database write operations in a retry mechanism (e.g., using a library like avast/retry-go or a custom backoff implementation) to handle transient database unavailability. Unit Testing: Write thorough unit tests for the RCA validation logic and state transitions to prove that a ticket cannot be closed without an RCA."
6. "You are forgetting that we already included component_id and severity in the work_items table in our init-postgres.sql file during the initial database setup. However, updating severity to an INT (e.g., 0 for P0, 1 for P1) is a smart choice for fast sorting on the Dashboard. Please update the Go models and rewrite the init-postgres.sql script to use an INT for severity instead of a VARCHAR. Once that is updated, proceed with generating the Go code for the Dashboard APIs, the strict State Machine validation (rejecting CLOSED without an RCA), the avast/retry-go database wrappers, and the unit tests."

**Phase 4: Frontend UI Generation**
7. "Yes, please build the frontend dashboard. I would like to use React (with Vite) and Tailwind CSS for styling. According to the requirements, the UI must have: Live Feed: A view showing active incidents sorted by severity. Incident Detail: Clicking an incident should show its current status and display the raw signals (fetched from the NoSQL data lake). RCA Form: A dedicated interface to submit an RCA. This MUST include Date-time pickers for Incident Start/End, a Dropdown for Root Cause Category, and Text areas for Fix Applied & Prevention Steps. Ensure the frontend correctly integrates with the backend APIs you just built. Please provide the setup instructions and the necessary React components."
8. "The implementation plan looks excellent. Here are the answers to your Open Questions: Tailwind: Yes, please use Tailwind CSS v3 as it is highly stable with Vite. MTTR Logic: YES, you MUST update the Go backend POST /api/v1/incidents/:id/rca endpoint. The assignment explicitly requires the frontend RCA form to include Date-time pickers for the Incident Start/End. Therefore, the backend must calculate the MTTR based on those explicit dates sent from the frontend, not just time.Now(). Please execute the plan. Update the Go backend endpoint first, then initialize the Vite React app and build out the frontend components."

**Phase 5: Final Packaging**
9. "The application is complete, the React UI looks great, and the Go backend is running perfectly. Now I need to prepare the repository for final submission according to the grading rubric. Please generate the following artifacts: README.md: This MUST include an Architecture Diagram (use Mermaid.js syntax so it renders beautifully), clear setup instructions using our docker-compose.yml, and a dedicated section explaining our Backpressure handling strategy (explain the Token Bucket rate limiter and the bounded Go channels). Sample Data Script: Create a simulate_failure.sh bash script designed to blast the http://localhost:8080/api/v1/signals ingestion endpoint. It should use curl, hey, or ab to send thousands of JSON payloads (simulating a sudden RDBMS outage followed by an MCP failure) to trigger the rate limiter and test the debouncing logic. Directory Check: Ensure the final workspace is cleanly separated into /backend and /frontend directories."

**Phase 6: Caching & Optimization**
10. "I reviewed `backend/api/dashboard.go` and realized we missed the 'Cache (The Hot-Path)' requirement from the architectural spec. The `GetIncidents` endpoint is querying PostgreSQL directly on every UI refresh. Please update `dashboard.go` to implement the caching layer using our Redis connection (`db.RedisClient`): 1. Read-Through Cache: In `GetIncidents`, try to fetch the active incidents from Redis first (key: `active_incidents`). If it's a cache miss, query Postgres, return the result to the user, and asynchronously save that JSON result into Redis with a 5-second TTL. 2. Cache Invalidation: In `UpdateStatus`, whenever a ticket's status is successfully updated, delete the `active_incidents` key from Redis so the frontend fetches fresh data on its next poll. Please output the refactored `dashboard.go` file."